#BST_Iterator.py
import ast

inp=input("enter the array: ")
inp=ast.literal_eval(inp)

values=input("enter the values array: ")
values=ast.literal_eval(values)

for i,j in zip(inp,values):
	if i[0]== "BSTIterator"
